# Input data

Large trajectories and derived source-data files are not stored in this GitHub
repository. Place local inputs here, or point the scripts to an external data
location with `COLLAGEN_DATA_ROOT` and the module-specific environment
variables documented in `docs/reproducibility.md`.
